---

machine:
  java:
    version: oraclejdk8
  environment:
    TERM: dumb

dependencies:
  override:
    - ./gradlew dependencies

test:
  override:
    - ./gradlew check
  post:
    - ./gradlew coveralls
